'use strict';

const contractServer = require('./src/contract-server');

module.exports = contractServer;

/*contractServer.initRestServer({
    port: 8000
});

contractServer.api.importAppsFiles('C:/nodejsws/rest-in-contract/samples/sample_import_app_folder');*/
